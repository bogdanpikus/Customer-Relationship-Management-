<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate x:Key="TextBoxPheceholder" TargetType="{x:Type TextBox}">
        <Grid>
            <Border x:Name="BorderTextBox" Background="AliceBlue" BorderBrush="Black" BorderThickness="2" CornerRadius="8">
                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
            </Border>
            <TextBlock 
                x:Name="Watermark"
                Text="{TemplateBinding Tag}"
                Foreground="Gray"
                Visibility="Collapsed"
                />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="Text" Value="">
                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
            </Trigger>
            <Trigger Property="IsFocused" Value="true">
                <Setter TargetName="BorderTextBox" Property="BorderBrush" Value="Aqua" />
                <Setter TargetName="Watermark" Property="Visibility" Value="Hidden" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>